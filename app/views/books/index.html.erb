<%= render 'books/new' %>

 <table>
   <tr>
     <th>投稿者</th><th>Title</th><th>Opinion</th>
   </tr>
 <% @books.each do |book| %>
   <tr>
     <th><%= image_tag book.user.get_profile_image(100,100) %></td>
     <td><%= link_to book.title, book_path(book.id) %></td>
     <td><%= book.body %></td>
   </tr>
 </table>

 <% end %>
